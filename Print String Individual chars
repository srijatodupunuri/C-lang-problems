#include <stdio.h>
#include <stdlib.h>

int main(){
    char name[100];
    int count=0;
    printf("\n Enter the name:");
    fgets(name,sizeof name,stdin);
    while(name[count]!='\0'){
        printf("%c ",name[count]);
        count++;
    }
    printf("\n");
    return 0;
}
