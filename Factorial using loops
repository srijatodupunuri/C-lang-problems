#include <stdio.h>

int main() {
    int i=1,fact=1,n;
    printf("Enter a no for which fact can be calculated: ");
    scanf("%d",&n);
    while(i<=n){
        fact*=i;
        i++;
    }printf("%d",fact);
    return 0;
}
