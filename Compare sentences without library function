// Online C compiler to run C program online
#include <stdio.h>
#include <string.h>

int main() {
      char sent[200],sent2[200];
      int i=0;
      printf("\n Enter any sentence 1: ");
      fgets(sent,sizeof (sent), stdin);
      //scanf("%s",sent);
      printf("\n Enter sentence 2: ");
      fgets(sent2,sizeof (sent2), stdin);
      //scanf("%s",sent2);
      for ( i = 0; sent[i] != '\0'&& sent2[i]!='\0'&& sent[i] == sent2[i]; i++) {
      }
      if (sent[i] == '\n') sent[i] = '\0';
      if (sent2[i] == '\n') sent2[i] = '\0';
      if (sent[i] == '\0'&& sent2[i]=='\0'){
                 printf("strings are equal\n");
              }else{
                 printf("Strings are not equal\n");
              }
          
      
      return 0;
}
