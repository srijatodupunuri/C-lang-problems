#include <stdio.h>

int main() {
    int array[10],sum=0;
    for(int i=0;i<5;i++){
       printf("Enter element:");
       scanf("%d",&array[i]);
    }
    for(int i=0;i<5;i++){
        sum+=array[i];
    }printf("Sum of array elements:%d",sum);
   
    return 0;
}
