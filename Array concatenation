#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int main() {
    int arr[100]={1,2,3,4,5};
    int arr2[100]={6,7,8,9,1};
    int arr3[100],l1=5,l2=5,l3=10,i,j,x;
    for(i=0;i<l1;i++){
        arr3[i]=arr[i];
    }
    for(j=0;j<l2;j++){
        arr3[l1+j]=arr2[j];
    }
    for(x=0;x<10;x++){
        printf("%d\n",arr3[x]);
    }
}
